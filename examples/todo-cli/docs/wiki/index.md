# Wiki index

- [Wiki schema](schema.md): the conventions every page follows.
- [Store as a single JSON file](decisions/json-file-store.md): why todos live in one `todos.json` written with write-then-rename.
- [The todo store](concepts/todo-store.md): how `load_store`, `save_store`, `add`, and `list` work together.
- [todo-cli delivery plan](sources/plan.md): what the signed-off delivery plan covers.
