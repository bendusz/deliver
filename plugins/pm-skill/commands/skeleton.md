---
description: Write or extend the SpecDD skeleton for a sprint, after sign-off and before decomposition.
---

Use the `project-manager` skill to write or extend the SpecDD skeleton. Load
`references/skeleton.md` and follow it: the trigger, the CLI probe, the `spec-architect` dispatch,
and the checkpoint are the whole contract.

Sprint: $ARGUMENTS  (a sprint number; default is the current sprint in `pm/pm-state.json`)

End by showing the tree of `.sdd` files written or superseded and the one-line entry you appended
to `pm/log.md`.
