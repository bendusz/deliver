# S<sprint>-<n> — <title>
<!-- pm-meta: {"builder":"<expert-builder|codex-builder|auto>","touches":["<repo-relative file or directory root>"]} -->
Sprint: <n> · Priority: <high|med|low> · Covers: <FR-…, AC-…> · Depends on: <ids|none> · Parallel-safe: <yes|no> · Touches: <comma-separated repo-relative files or directory roots; no globs; or —>
Risk: <low|med|high> · Review lenses: <code-integrity-reviewer[, architecture-reviewer][, security-auditor]> · Security-sensitive: <yes|no> · Architecture-sensitive: <yes|no>
Builder: <expert-builder|codex-builder|auto>

## Goal
<one paragraph>

## Context (self-contained)
<architecture, files, interfaces, conventions the builder needs — so it need not read the whole repo>

## Acceptance criteria (testable)
- [ ] <criterion>

## Out of scope
- <item>

## Verification
- Prove done with: `<command>`

## Verification evidence
<!-- Filled after the verify step: pm-verifier STATUS (PASS/FAIL/UNKNOWN), gate results, and a link to
     docs/verification/<story-id>.md if one was written. Empty until the story is verified. -->
