# <Project> project constitution

<!--
Rules that are specific to THIS project and go beyond the pm-skill hard rules. The hard rules
(sign-off, separate reviewer, deterministic gates, verifier PASS, scope freeze, repository safety,
traceability) already apply and must not be restated here. This file never weakens them; where it
and a request conflict, the stricter rule wins. Every line should be checkable by /pm-skill:analyze.
Delete any section this project has nothing to say about. If there is nothing at all, write
"No additional project-specific rules." and keep the file short.
-->

## Principles
- <a core value that guides trade-offs, e.g. "correctness over speed">
<!--
If the poteto companion plugin is installed, its 21 principle skills are a ready-made menu: name
the ones this project adopts and /pm-skill:analyze can check stories against them.
Core: laziness protocol, foundational thinking, redesign from first principles, subtract before
you add, minimize reader load, outcome-oriented execution, experience first, exhaust the design
space, build the lever. Architecture: model the domain, boundary discipline, type system
discipline, make operations idempotent, migrate callers then delete legacy APIs, separate before
serializing shared state. Verification: prove it works, fix root causes, sequence work into
verifiable units. Delegation: guard the context window, never block on the human (pm-skill's
sign-off gate still wins). Meta: encode lessons in structure.
-->

## Engineering standards
- <language/style conventions, error-handling expectations, dependency policy>

## Testing standards
- <what must be tested, coverage expectations, test-first where practical>

## Security & privacy requirements
- <authn/authz rules, secret handling, data/PII handling>

## UX / product constraints
- <accessibility, supported platforms, performance budgets, tone>
