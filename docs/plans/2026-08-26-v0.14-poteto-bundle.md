# v0.14 poteto companion plugin implementation plan

Date: 2026-08-26
Specification: `docs/specs/2026-08-26-v0.14-poteto-bundle.md`

## 1. Plugin scaffold

- [x] Create `plugins/poteto/.claude-plugin/plugin.json` (name, version 0.14.3, MIT, author, homepage,
  repository, keywords).
- [x] Copy upstream `pstack/LICENSE` to `plugins/poteto/LICENSE` unmodified.
- [x] Add the `poteto` entry to `.claude-plugin/marketplace.json`.

## 2. Skills

- [x] Copy the nine already-ported skills and the 21 principles from `~/.claude/skills/` into
  `plugins/poteto/skills/`.
- [x] Copy `interrogate`, `technical-writing`, `maintain-verification-skill`, `reflect`, `teach`,
  `typescript-best-practices` from upstream at the recorded SHA.
- [x] Patch the six new skills per the patch rules (models, subagent params, config lookups, paths,
  tool names).
- [x] Apply the namespacing patch across every skill body (`/how` to `/poteto:how` and so on).
- [x] Confirm no Cursor-only string remains.

## 3. Attribution and port record

- [x] Write `plugins/poteto/README.md` (tribute, scope, changes, install, re-sync pointer).
- [x] Write `plugins/poteto/PORT.md` (upstream SHA, patch table by skill and class, exclusions with
  reasons, re-sync recipe).

## 4. pm-skill integration

- [x] `references/planning-and-signoff.md`: optional design-exploration step.
- [x] `references/decomposition.md`: optional `architect` pass for Architecture-sensitive stories.
- [x] `references/review-gates.md`: triage buckets and filtering rules; optional `blast-radius` lens.
- [x] `references/implementation-loop.md`: PR body sections at ship.
- [x] `references/verification.md`: drive a project `verify-<app>` skill when present.
- [x] `commands/handoff.md`: log-to-evidence check before writing the handoff.
- [x] `agents/technical-writer.md`: apply a technical-writing skill when installed.
- [x] `templates/constitution.md.template`: principle menu.

## 5. Documentation and release

- [x] README install line, Optional enhancements paragraph, Safety note.
- [x] `docs/prior-art.md` pstack entry.
- [x] `CHANGELOG.md` 0.14.0 and `plugin.json` bump.

## 6. Validation

- [x] Extend `scripts/validate.sh` for the second plugin.
- [x] Run `bash scripts/validate.sh` and `shellcheck` on hooks and scripts.
- [x] Diff `~/.claude/skills/*` against `plugins/poteto/skills/*` and confirm only namespacing differs.
- [x] Commit on `feat/poteto-bundle`.
