# v0.14 poteto companion plugin implementation plan

Date: 2026-08-26
Specification: `docs/specs/2026-08-26-v0.14-poteto-bundle.md`

## 1. Plugin scaffold

- [ ] Create `plugins/poteto/.claude-plugin/plugin.json` (name, version 0.14.3, MIT, author, homepage,
  repository, keywords).
- [ ] Copy upstream `pstack/LICENSE` to `plugins/poteto/LICENSE` unmodified.
- [ ] Add the `poteto` entry to `.claude-plugin/marketplace.json`.

## 2. Skills

- [ ] Copy the nine already-ported skills and the 21 principles from `~/.claude/skills/` into
  `plugins/poteto/skills/`.
- [ ] Copy `interrogate`, `technical-writing`, `maintain-verification-skill`, `reflect`, `teach`,
  `typescript-best-practices` from upstream at the recorded SHA.
- [ ] Patch the six new skills per the patch rules (models, subagent params, config lookups, paths,
  tool names).
- [ ] Apply the namespacing patch across every skill body (`/how` to `/poteto:how` and so on).
- [ ] Confirm no Cursor-only string remains.

## 3. Attribution and port record

- [ ] Write `plugins/poteto/README.md` (tribute, scope, changes, install, re-sync pointer).
- [ ] Write `plugins/poteto/PORT.md` (upstream SHA, patch table by skill and class, exclusions with
  reasons, re-sync recipe).

## 4. pm-skill integration

- [ ] `references/planning-and-signoff.md`: optional design-exploration step.
- [ ] `references/decomposition.md`: optional `architect` pass for Architecture-sensitive stories.
- [ ] `references/review-gates.md`: triage buckets and filtering rules; optional `blast-radius` lens.
- [ ] `references/implementation-loop.md`: PR body sections at ship.
- [ ] `references/verification.md`: drive a project `verify-<app>` skill when present.
- [ ] `commands/handoff.md`: log-to-evidence check before writing the handoff.
- [ ] `agents/technical-writer.md`: apply a technical-writing skill when installed.
- [ ] `templates/constitution.md.template`: principle menu.

## 5. Documentation and release

- [ ] README install line, Optional enhancements paragraph, Safety note.
- [ ] `docs/prior-art.md` pstack entry.
- [ ] `CHANGELOG.md` 0.14.0 and `plugin.json` bump.

## 6. Validation

- [ ] Extend `scripts/validate.sh` for the second plugin.
- [ ] Run `bash scripts/validate.sh` and `shellcheck` on hooks and scripts.
- [ ] Diff `~/.claude/skills/*` against `plugins/poteto/skills/*` and confirm only namespacing differs.
- [ ] Commit on `feat/poteto-bundle`.
