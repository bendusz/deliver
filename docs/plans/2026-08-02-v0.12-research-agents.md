# pm-skill v0.12 Research Agents Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add two agents to the pm-skill plugin — `researcher` (web-capable external research, sourced reports under `docs/research/`) and `codex-researcher` (independent second-model opinion via the OpenAI Codex CLI) — and route the workflow's research guidance to them.

**Architecture:** Two new agent markdown files following the existing nine's structure (frontmatter: name/description/tools/model/effort/color + prompt body with Hard limits / Method / Done means / Return sections). Doc edits swap the "dispatch built-in `general-purpose`/`Explore`" guidance for the new agents. No hooks, templates, or commands change.

**Tech Stack:** Markdown agent definitions, `scripts/validate.sh` as the deterministic gate, Codex CLI (`codex exec`) invoked by the codex-researcher agent at its own runtime.

**Source spec:** `docs/specs/2026-08-02-v0.12-research-agents.md` (approved).

## Global Constraints

- Branch: all work on `pm-skill-v0.12.0` (already created; spec committed as d7e8917).
- `plugins/pm-skill/.claude-plugin/plugin.json` `version` must equal the top CHANGELOG entry (`0.12.0`) — validate.sh check 11.
- Agent frontmatter `description:` values are unquoted YAML plain scalars — they must NOT contain `: ` (colon+space) anywhere — validate.sh check 13.
- Never rewrite old CHANGELOG entries or past specs/plans — append/amend with dated notes only.
- The Bash tool's shell is zsh: no word-splitting of unquoted vars; write multi-flag command lines literally; avoid bare `echo ===`.
- Stage files explicitly by path in every commit (`git add <paths>`), never `git add -A`.
- Commit messages end with `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Gate for every task: `scripts/validate.sh` exits 0.

---

### Task 1: Verify codex `--search` support and document it

**Files:**
- Modify: `docs/codex-cli-reference.md` (append a dated subsection; do not rewrite existing text)

**Interfaces:**
- Produces: a documented fact — whether `codex exec` supports `--search` — that Task 3's agent body relies on (the agent also probes `codex exec --help` at its own runtime, so either outcome is fine).

- [ ] **Step 1: Probe the installed CLI**

Run:
```bash
codex --version && codex exec --help | grep -i -- '--search'
```
Expected: version string, then either a `--search` help line (supported) or no grep match (unsupported). If `codex` is missing entirely, note that and document from the CLI's official docs instead (`codex exec --help` output in CI/other machines may differ — say so).

- [ ] **Step 2: Append a dated subsection to `docs/codex-cli-reference.md`**

Append at the end of the file (adjust the finding to what Step 1 actually showed — the text below assumes supported; if unsupported, say "does NOT accept `--search` as of <version>" and that codex-researcher falls back to model knowledge + repo reading):

```markdown

## Web search (`--search`) — noted 2026-08-02, codex <version from step 1>

`codex exec` accepts `--search`, enabling Codex's native web-search tool for the run. The
`codex-researcher` agent probes `codex exec --help` for this flag at runtime and adds it when
present, so external research questions get live sources on installations that support it and
degrade to model knowledge + repo reading on ones that don't. `codex exec review` does not take
`--search`.
```

(Verify the `codex exec review` claim with `codex exec review --help | grep -i -- '--search'` and adjust if it does accept it.)

- [ ] **Step 3: Validate and commit**

```bash
scripts/validate.sh && git add docs/codex-cli-reference.md && git commit -m "docs: codex --search support in CLI reference

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```
Expected: validate.sh exits 0; one file committed.

---

### Task 2: Create the `researcher` agent

**Files:**
- Create: `plugins/pm-skill/agents/researcher.md`

**Interfaces:**
- Produces: agent name `researcher`, report path convention `docs/research/YYYY-MM-DD-<slug>.md`, digest ≤ 20 lines — Tasks 4–7 reference these exactly.

- [ ] **Step 1: Write `plugins/pm-skill/agents/researcher.md`**

Exact content (note — the `description` value contains no colon+space; keep it that way if you adjust wording):

````markdown
---
name: researcher
description: Use PROACTIVELY during discovery, specification, and planning — and when a story is blocked on an unknown external dependency — to answer one tightly-scoped external research question (library/framework choice, SDK/API facts, prior art, best practices) with sourced findings and an explicit recommendation. Writes a report under docs/research/ and returns a short digest. <example>The user must choose between two auth libraries, so the PM dispatches researcher with the question and constraints; it writes docs/research/2026-08-02-auth-library.md and returns a digest recommending one.</example>
tools: Read, Grep, Glob, WebSearch, WebFetch, Write
model: sonnet
effort: medium
color: cyan
---

You are an external-research analyst. You answer ONE tightly-scoped research question per
dispatch — a library or framework choice, SDK/API facts, prior art, best practices, an
ecosystem scan — and you write the findings down so they outlive the session.

## Hard limits
- You may create or update files under `docs/research/` ONLY. Never touch source, tests, or
  config — your Write access exists solely for research reports.
- Research, don't decide for the project. You return a recommendation; the PM and the user own
  the decision.

## Method
1. Restate the question and the decision it feeds. If the brief isn't answerable as scoped,
   say so immediately instead of researching the wrong thing.
2. Ground yourself in the repo first (Read/Grep/Glob) — what's already in use, versions in
   lockfiles/manifests, existing patterns the answer must fit.
3. Search the web (WebSearch) and read primary sources (WebFetch) — official docs, release
   notes, changelogs, maintainer statements. Prefer primary sources over blog posts; note the
   publication date of anything that moves fast.
4. Weigh the realistic options — usually 2–3 — against the constraints in the brief.

If the web is unreachable (tool denied, offline) — answer from the repo and model knowledge,
mark every claim you could not verify as **unverified**, and say the web was unavailable.
Never invent sources.

## Report — `docs/research/YYYY-MM-DD-<slug>.md`
Today's date, short kebab-case slug of the question. Sections:
- **Question** — and the decision it feeds.
- **Context** — repo facts that constrain the answer (versions, patterns), with file paths.
- **Findings** — every claim cites its source (URL or file path). No uncited claims.
- **Options** — 2–3, each with honest trade-offs.
- **Recommendation** — one, explicit, with the reasoning.
- **Confidence & gaps** — what you could not verify and what could change the answer.

## Done means (completion criteria)
- The report exists at its `docs/research/` path with all six sections filled.
- Every Findings claim carries a source; unknowns are stated, not papered over.
- The recommendation is explicit — "it depends" without a default is not done.

## Return — a digest (≤ 20 lines)
- The recommendation (1–2 lines) and why (2–3 lines).
- Top findings (≤ 3 bullets).
- Confidence and the biggest gap.
- The report path.
Never paste the full report back.
````

- [ ] **Step 2: Validate**

```bash
scripts/validate.sh
```
Expected: exit 0 (checks 4 and 13 now cover the new file).

- [ ] **Step 3: Commit**

```bash
git add plugins/pm-skill/agents/researcher.md
git commit -m "feat: researcher agent — web-capable external research with sourced reports

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: Create the `codex-researcher` agent

**Files:**
- Create: `plugins/pm-skill/agents/codex-researcher.md`

**Interfaces:**
- Consumes: the `--search` fact from Task 1 (via its own runtime `codex exec --help` probe).
- Produces: agent name `codex-researcher`, report convention `docs/research/YYYY-MM-DD-<slug>-codex.md`, the UNAVAILABLE digest contract — Tasks 4–7 reference these exactly.

- [ ] **Step 1: Write `plugins/pm-skill/agents/codex-researcher.md`**

Exact content (description has no colon+space):

````markdown
---
name: codex-researcher
description: Use for an independent second-model research opinion via the OpenAI Codex CLI — risk-selected alongside researcher for consequential or contested questions (framework choice, security posture, anything the user flags). Requires the codex binary and returns UNAVAILABLE cleanly when it is missing. Writes an attributed report under docs/research/. <example>The framework choice is contested, so the PM dispatches codex-researcher with the same brief given to researcher and synthesizes the two reports.</example>
tools: Read, Grep, Glob, Bash, Write
model: sonnet
effort: medium
color: purple
---

You are a research liaison to **Codex** (OpenAI's coding-agent CLI). The research thinking
happens inside Codex — an independent second model. You compose the brief, run the binary
safely, and distill the result. You never answer the research question from your own knowledge.

## Hard limits
- You may create or update files under `docs/research/` ONLY.
- Never pass `--dangerously-bypass-approvals-and-sandbox`, `--full-auto`, or `--yolo`.
- Codex output lands outside the repo (mktemp); the only file you add to the repo is the final
  report you write yourself.

## 1. Preflight
1. `command -v codex` — missing → return the UNAVAILABLE digest (below); this is a clean
   result, not an error.
2. `codex login status` — non-zero exit → UNAVAILABLE digest telling the user to run
   `codex login` (or `printenv OPENAI_API_KEY | codex login --with-api-key`).

## 2. Compose the brief
Codex can read the repo but knows nothing about this conversation. Write a self-contained
brief from the dispatch prompt: the question and the decision it feeds, the relevant file
paths, the options being weighed with their trade-offs, and the constraints (conventions,
versions, non-negotiables). End with — `Give a concrete recommendation and your reasoning.
Read the referenced files before answering, and cite sources for external claims.`

## 3. Run
Defaults — model `gpt-5.6-terra`, effort `high`; use values from the dispatch prompt when
given. Create `SCRATCH=$(mktemp -d)` so Codex output stays outside the repo. If
`codex exec --help` lists `--search`, include that flag (live web sources). From the repo root:

    codex exec --sandbox read-only --ephemeral --color never \
      -m "$MODEL" -c model_reasoning_effort="$EFFORT" \
      -o "$SCRATCH/answer.md" "<brief>" 2>"$SCRATCH/stderr.log"

Non-zero exit → put the cause from `stderr.log` (auth, usage error) and the scratch paths in
your digest for inspection; retry at most once, and only for transient failures.

## 4. Report — `docs/research/YYYY-MM-DD-<slug>-codex.md`
- **Question** — plus model/effort used and whether `--search` was available.
- **Codex's findings and recommendation** — attributed throughout ("Codex (gpt-5.6-terra)
  finds …"), distilled from the scratch answer, never pasted wholesale.
- **Caveats** — the few places Codex's answer seems weak, unsupported, or stale. Nothing else
  of your own — the PM owns synthesis with the `researcher` report.

## Done means (completion criteria)
- Either the report exists with all three sections and the digest cites its path, or the
  digest is a clean UNAVAILABLE/failure result with the reason and the exact fix command.

## Return — a digest (≤ 15 lines)
- Codex's recommendation (attributed), your caveats, the report path.
- Unavailable form — `UNAVAILABLE — <reason>` plus the install command
  (`npm install -g @openai/codex` or `brew install codex`) or `codex login`.
````

- [ ] **Step 2: Validate**

```bash
scripts/validate.sh
```
Expected: exit 0.

- [ ] **Step 3: Commit**

```bash
git add plugins/pm-skill/agents/codex-researcher.md
git commit -m "feat: codex-researcher agent — second-model research opinion via Codex CLI

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: Wire both agents into SKILL.md and the model-tiering reference

**Files:**
- Modify: `plugins/pm-skill/skills/project-manager/SKILL.md:72-73` (agents list), `:76-78` (codex paragraph), `:79-82` (tiering summary)
- Modify: `plugins/pm-skill/skills/project-manager/references/model-tiering.md:5` (heading), `:21-22` (table rows)

**Interfaces:**
- Consumes: agent names and report conventions from Tasks 2–3, exactly as produced.

- [ ] **Step 1: SKILL.md agents list**

Replace lines 72–73:
```markdown
- `codebase-analyst` — read-only; maps an existing codebase into a context pack for planning/stories.
- For read-only research, dispatch the built-in `general-purpose` (or `Explore`) agent.
```
with:
```markdown
- `codebase-analyst` — read-only; maps an existing codebase into a context pack for planning/stories.
- `researcher` — web-capable external research (libraries, SDKs, prior art); one scoped question per dispatch, sourced report under `docs/research/`, short digest back.
- `codex-researcher` — optional second-model research opinion via the OpenAI Codex CLI; risk-selected alongside `researcher` for consequential or contested questions; returns UNAVAILABLE cleanly when `codex` is absent.
```

- [ ] **Step 2: SKILL.md codex paragraph**

In the paragraph at lines ~76–78, after "…offers a one-off second opinion on a consequential decision — use sparingly.", append:
```markdown
The `codex-researcher` agent plays the same independent-second-model role for research questions.
```

- [ ] **Step 3: SKILL.md tiering summary**

In the sentence at lines ~79–82, change
```markdown
with `codebase-analyst` and `technical-writer` on
`sonnet`.
```
to
```markdown
with the breadth roles — `codebase-analyst`, `technical-writer`, `researcher`,
`codex-researcher` — on `sonnet`.
```

- [ ] **Step 4: model-tiering.md**

Change the heading `## Shipped defaults (v0.11)` to `## Shipped defaults (v0.12)`, and add two rows after the `technical-writer` row:
```markdown
| `researcher` | `sonnet` | `medium` | web research; breadth and sourcing over depth |
| `codex-researcher` | `sonnet` | `medium` | thin wrapper — the thinking happens inside Codex |
```

- [ ] **Step 5: Verify and commit**

```bash
scripts/validate.sh && ! grep -n 'general-purpose' plugins/pm-skill/skills/project-manager/SKILL.md
git add plugins/pm-skill/skills/project-manager/SKILL.md plugins/pm-skill/skills/project-manager/references/model-tiering.md
git commit -m "feat: dispatch and tiering wiring for the research agents

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```
Expected: validate.sh 0; the grep finds nothing (inverted — command exits 0 overall).

---

### Task 5: Route the phase references to `researcher`

**Files:**
- Modify: `plugins/pm-skill/skills/project-manager/references/discovery.md:21-25`
- Modify: `plugins/pm-skill/skills/project-manager/references/specification.md:36-39`
- Modify: `plugins/pm-skill/skills/project-manager/references/planning-and-signoff.md:5-8`

**Interfaces:**
- Consumes: agent names from Tasks 2–3.

- [ ] **Step 1: discovery.md "Protect your context"**

Replace lines 22–25 (keep the section heading):
```markdown
- You are the PM. Keep the conversation and the decisions in this thread.
- For anything that needs heavy reading (existing code, docs, prior art), dispatch the built-in
  `general-purpose` (or `Explore`) subagent with a tight question and have it return a short
  summary. Don't read large sources into your own context.
```
with:
```markdown
- You are the PM. Keep the conversation and the decisions in this thread.
- For anything that needs heavy reading, dispatch a read-only subagent with a tight question and
  take back a short summary — `researcher` for external questions (libraries, prior art, best
  practices; it writes its findings under `docs/research/`), `codebase-analyst` for the existing
  code. Don't read large sources into your own context.
```

- [ ] **Step 2: specification.md "Protect your context"**

Replace lines 37–39:
```markdown
For heavy reading (prior art, an existing codebase, external docs), dispatch a read-only
`general-purpose`/`Explore` subagent with a tight question and take back a short summary — don't read
large sources into your own context.
```
with:
```markdown
For heavy reading, dispatch a read-only subagent with a tight question and take back a short
summary — `researcher` for prior art and external docs (findings land under `docs/research/`),
`codebase-analyst` for an existing codebase. Don't read large sources into your own context.
```

- [ ] **Step 3: planning-and-signoff.md section 0**

After line 8 ("Skip this for a greenfield project."), append to the same section:
```markdown
If the plan hinges on an external unknown (a library choice, an unfamiliar SDK), dispatch
`researcher` — and `codex-researcher` alongside it for a consequential or contested call when
the codex CLI is installed — then cite the `docs/research/` reports in the plan's decisions.
```

- [ ] **Step 4: Verify and commit**

```bash
! grep -rn 'general-purpose' plugins/pm-skill/skills/project-manager/references/
scripts/validate.sh
git add plugins/pm-skill/skills/project-manager/references/discovery.md plugins/pm-skill/skills/project-manager/references/specification.md plugins/pm-skill/skills/project-manager/references/planning-and-signoff.md
git commit -m "docs: phase references route research to the new agents

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```
Expected: grep empty, validate.sh 0.

---

### Task 6: README roster

**Files:**
- Modify: `README.md:64-69`

- [ ] **Step 1: Extend the bundled-agents paragraph**

Replace (lines 64–69):
```markdown
a **`codebase-analyst`** for brownfield work. The PM stays an orchestrator and protects its own
context by handing each agent only what it needs.
```
with:
```markdown
a **`codebase-analyst`** for brownfield work, and two research roles — a web-capable
**`researcher`** and an optional Codex-CLI-backed **`codex-researcher`** (independent
second-model opinion) — whose sourced reports land under `docs/research/`. The PM stays an
orchestrator and protects its own context by handing each agent only what it needs.
```

- [ ] **Step 2: Verify and commit**

```bash
scripts/validate.sh && git add README.md && git commit -m "docs: README agent roster — research agents

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 7: Version bump + CHANGELOG

**Files:**
- Modify: `plugins/pm-skill/.claude-plugin/plugin.json` (`"version": "0.11.0"` → `"0.12.0"`)
- Modify: `CHANGELOG.md` (new top entry; never touch old entries)

- [ ] **Step 1: Bump plugin.json version to `0.12.0`**

- [ ] **Step 2: Insert the CHANGELOG entry above `## 0.11.0`**

```markdown
## 0.12.0 — <date of the Task 7 commit, YYYY-MM-DD>

Research agents — the fleet gains an external-research role and an optional second-model
counterpart; findings persist under `docs/research/` instead of dying with the session.

- **New `researcher` agent** (`sonnet`/`medium`) — web-capable (WebSearch/WebFetch); answers one
  tightly-scoped external question per dispatch (library choice, SDK facts, prior art) with
  sourced findings and an explicit recommendation; writes `docs/research/YYYY-MM-DD-<slug>.md`
  and returns a ≤20-line digest. Write access is prompt-constrained to `docs/research/` only.
- **New `codex-researcher` agent** (`sonnet`/`medium`) — drives `codex exec --sandbox read-only
  --ephemeral` (default `gpt-5.6-terra` @ `high`, `--search` when the CLI supports it) for an
  independent second-model research opinion; risk-selected alongside `researcher` for
  consequential or contested questions, mirroring codex-review vs the review panel; returns
  UNAVAILABLE cleanly when the codex CLI is missing; attributed report at
  `docs/research/YYYY-MM-DD-<slug>-codex.md`.
- **Dispatch guidance rerouted** — SKILL.md and the discovery/specification/planning references
  now send research to `researcher` instead of the built-in `general-purpose`/`Explore` agents.
- **Docs** — model-tiering table extended (both new agents on `sonnet`/`medium`); README roster
  updated; codex `--search` support recorded in `docs/codex-cli-reference.md`.
```

(Adjust the `--search` clauses in both the codex-researcher bullet and the Docs bullet to match Task 1's actual finding.)

- [ ] **Step 3: Full gate and commit**

```bash
scripts/validate.sh
git add plugins/pm-skill/.claude-plugin/plugin.json CHANGELOG.md
git commit -m "chore: release v0.12.0 — research agents

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```
Expected: validate.sh 0 (check 11 now passes on the 0.12.0 pair).

---

### Task 8: Release (Ben's standard ritual)

No new files — this is the shipped release flow; each step gates the next.

- [ ] **Step 1: Codex review rounds** — from the repo root, background, stderr to a sidecar log:
  `codex exec review --base main -m gpt-5.6-terra -c 'model_reasoning_effort="high"' --ephemeral -o "$(mktemp -d)/codex-round-N.md"` (keep the temp path; stderr to a sidecar log beside it) — fix every finding, commit as `fix: … (codex round N)`, repeat until a round is clean or only recycles settled P2 judgment calls.
- [ ] **Step 2: Merge** — `git checkout main && git merge --no-ff pm-skill-v0.12.0 -m "Merge pm-skill v0.12.0: research agents (researcher + codex-researcher)"`; delete the branch.
- [ ] **Step 3: Tag + release** — annotated `v0.12.0` tag; push main+tag; `gh release create v0.12.0` with notes extracted via `awk '/^## 0.12.0/{flag=1;next}/^## /{flag=0}flag' CHANGELOG.md`.
- [ ] **Step 4: Local update** — `claude plugin marketplace update pm-skill` then `claude plugin update pm-skill@pm-skill`; verify `~/.claude/plugins/cache/pm-skill/pm-skill/0.12.0/` matches the repo tree (`diff -rq`). Ben runs `/reload-plugins` and initiates the probe (dispatch each new agent with a real question; codex-researcher must return UNAVAILABLE cleanly with codex PATH-masked).
