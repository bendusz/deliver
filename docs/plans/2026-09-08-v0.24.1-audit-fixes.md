# v0.24.1 audit fixes Implementation Plan

**Goal:** Land the audit's defects as a fix release. **Spec:** `docs/specs/2026-09-08-v0.24.1-audit-fixes.md`.

- [x] Runtime: hook content hashing and common git dir in `snapshot.mjs`; `--base` optional under preflight; empty-diff handoff freshness; comment fixes.
- [x] Prose: every row of the spec's table.
- [x] `validate.sh` legacy scan over plugins, docs, and the example.
- [x] Version 0.24.1 and the CHANGELOG entry.
