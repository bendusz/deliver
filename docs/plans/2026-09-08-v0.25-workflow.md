# v0.25 workflow Implementation Plan

**Goal:** Land the audit's two recommendations and two optimisations. **Spec:** `docs/specs/2026-09-08-v0.25-workflow.md`.

- [x] `lib.mjs`: `inspectState`, `PHASES`, the `state` CLI, `chomp` exported.
- [x] `session-context.mjs` renders `inspectState` and prints the phase line.
- [x] `require-signoff.mjs` and `build.mjs` enforce `plan_digest`.
- [x] `retrospective.md`, `commands/retro.md`, `retro.md.template`; checkpoint policy and SKILL.md name them.
- [x] Enter and Leave lines on the five phase references; analyze after decomposition.
- [x] `AGENTS.md` dropped from agent Inputs and the Codex prompt; `instruction-layers.md` rule.
- [x] Tests for the phase line, the state CLI, and the digest gate in hook and runner.
- [x] README, CHANGELOG, version 0.25.0.
