# v0.24 git is the state Implementation Plan

**Goal:** Replace the tracked `pm/` state directory with an approval marker, per-story Execution
blocks, and tracked handoffs, so git carries everything else, released as 0.24.0.

**Spec:** `docs/specs/2026-09-07-v0.24-git-as-state.md`

## Global Constraints

- Runtime code is Node ESM only; hooks stay fail-open.
- The actor-id salt in `hooks/lib.mjs` does not change.
- `bash scripts/validate.sh` ends `validate.sh: OK` and `node --test plugins/deliver/scripts/tests/`
  is green at the end.
- Branch `feat/v0.24-git-as-state`, cut from `feat/v0.23-no-sandbox`. Never push.

### Task 1: Hooks and library
- [ ] `lib.mjs`: `APPROVAL_REL`, `readApproval`, `parseExec`, `legacyState`.
- [ ] `require-signoff.mjs`: marker first, legacy fallback.
- [ ] `pm-secrets-guard.mjs`: `docs/` and `pm/`.
- [ ] Delete `actor-guard.mjs`; update `hooks.json`.
- [ ] `session-context.mjs`: git-derived context.
- [ ] `helpers.mjs`, `hooks.test.mjs`, `lib.test.mjs` updated.

### Task 2: Codex runner
- [ ] `build.mjs`: approval check, protected paths, prompt; `snapshot.mjs`: protected paths;
  `smoke-live.mjs`; `codex-run.test.mjs`.

### Task 3: Skill, references, commands, templates
- [ ] `state.md` replaces `logging-and-state.md`; every reference and command in the spec's section F.
- [ ] Templates added, changed, deleted per section F.

### Task 4: Docs, example, release
- [ ] README, `docs/codex-cli-reference.md`, repo `AGENTS.md`, `examples/`, `validate.sh`.
- [ ] `plugin.json` 0.24.0 and the CHANGELOG entry.

### Task 5: Review rounds
- [ ] Two Codex (`gpt-6-astra`, high) review rounds over the squashed commit, fixes between.
