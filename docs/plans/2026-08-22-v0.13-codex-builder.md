# v0.13 Codex precision builder implementation plan

Date: 2026-08-22
Specification: `docs/specs/2026-08-22-v0.13-codex-builder.md`

## 1. Agent and routing surface

- [x] Add a thin Sonnet `codex-builder` agent that invokes only the bundled runner.
- [x] Add `Builder: expert-builder | codex-builder | auto` to the story format and readiness rules.
- [x] Keep Opus `expert-builder` as the broad-context default.
- [x] Add evidence-based Codex fix routing without resetting existing loop bounds.

## 2. Safe runner

- [x] Add strict named argument parsing and canonical worktree, story, and evidence validation.
- [x] Fail closed for missing, symlinked, malformed, or unsigned PM state.
- [x] Enforce strict story `pm-meta.touches` as a repository path allowlist and cross-check the
  visible Builder and Touches fields.
- [x] Pin the worktree, sandbox, model, effort, schema, and output mode.
- [x] Ignore user config and execpolicy rules; disable network, web, MCP servers, and hooks with
  higher-precedence session overrides.
- [x] Derive authoritative changed paths from before/after content fingerprints.
- [x] Reject protected, out-of-scope, and unreported repository edits.
- [x] Detect HEAD, any ref, staged-content, local-config, and worktree-registration mutation.
- [x] Add bounded runtime, signal handling, descendant termination, and partial-change reporting.
- [x] Retain diagnostics only for unsuccessful runs.
- [x] Reduce and secret-filter the tool environment; exclude host temp roots and supply a cleaned
  worktree-local runtime temp directory.
- [x] Add a quota-free `--preflight` readiness and optional story-scope check.

## 3. Resumable orchestration

- [x] Persist `resolved_builder` in sequential actor state and clear it after ship.
- [x] Persist `builder` in each parallel batch entry.
- [x] Make route logging part of the clean claim transaction.
- [x] Give parallel `expert-builder` dispatches the absolute worktree and require exact-root checks.
- [x] Use cumulative runner-derived paths for Codex diff, review, fix, verify, and ship operations.
- [x] Expose the persisted route in session context and resume instructions.

## 4. Validation

- [x] Add a JSON result schema and executable-runner validation.
- [x] Add quota-free stub tests for happy paths, blocked paths, safety violations, dirty baselines,
  hostile config, timeouts, cleanup, and Git metadata changes.
- [x] Add an opt-in live Codex smoke test in a disposable signed-off repository. It is not part of
  default validation because it consumes quota and depends on local authentication.
- [x] Run the live smoke successfully against codex-cli 0.149.0 on 2026-08-23.
- [x] Add an isolated builder benchmark command, scoring script, result schema, and routing cases.
- [x] Run hook tests, builder tests, plugin validation, ShellCheck, syntax checks, and diff checks.

## 5. Documentation

- [x] Update README, changelog, agent roster, doctor checks, model tiering, decomposition, sequential
  and parallel loops, state documentation, templates, and the Codex CLI reference.
- [x] Document host-temp exclusion, environment isolation, and the remaining trusted-project and
  OS-sandbox boundary.
- [x] Add this plan and its matching v0.13 specification.
