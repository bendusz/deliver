# v0.15 Opus focus implementation plan

Specification: `docs/specs/2026-08-27-v0.15-opus-focus.md`

## Release slice

- [x] Recover and retain the Opus performance research report.
- [x] Pin every gate-bearing Opus agent to `claude-opus-5`.
- [x] Set role-appropriate `high` and `medium` defaults with no shipped `xhigh` or `max` Opus role.
- [x] Add a concise, role-specific focus contract to every Opus agent.
- [x] Make the PM derive writer changes from Git and reject paths outside `pm-meta.touches`.
- [x] Add Claude model and effort regime checks to doctor.
- [x] Preserve pre-compaction checkpoint and post-resume state verification guidance.
- [x] Keep Opus fleet drift checks inside the existing validation script.
- [x] Remove the Opus benchmark command, schema, scorer, and standalone test script.
- [x] Update model-tiering guidance, README, changelog, research, and plugin version.
- [x] Run repository validation and inspect the final diff. Opus/static checks, hook tests, and
  builder-benchmark tests pass. The full validator still exposes two unchanged macOS-sensitive
  Codex-runner test failures that reproduce against the committed baseline.

## Deferred work

- Fail-closed approval and consequential-write hooks. This needs a separate compatibility and
  migration design with adversarial hook tests.
- Scheduler-level token, cost, tool-call, and subagent concurrency circuit breakers.
- Provider-specific dated model IDs and organization-specific model mappings.
- A representative external evaluation suite for deciding whether any role should use deeper
  effort in a future release.
