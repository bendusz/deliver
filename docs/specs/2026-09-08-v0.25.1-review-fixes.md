# v0.25.1: Codex review fixes

Status: approved by the user on 2026-09-08 ("if all clear pr and merge them one by one"), from
a Codex (gpt-6-astra, high) review of 0.24.1 and 0.25.0 after they had merged. Writing standard:
`plugins/poteto/skills/unslop/SKILL.md`.

| Severity | Where | Defect | Fix |
|---|---|---|---|
| Major | `hooks/lib.mjs` | Phase derivation honoured revocation only beside a plan file and never looked at `plan_changed` | Unapproved or drifted marker is `planning` first |
| Major | `hooks/lib.mjs`, references | `done` and resume ignored the sprint retrospective | New `retrospective` phase, skipped at `tiny` and `small`; resume claims nothing while owed |
| Major | `hooks/lib.mjs` | Unreadable stories were dropped, producing a false `done` | Counted as unmerged, listed under `unreadable` |
| Major | `hooks/lib.mjs` | `state <root>` went through `pmRoot`, so `CLAUDE_PROJECT_DIR` won | Explicit root is authoritative |
| Major | `modes/build.mjs`, `require-signoff.mjs` | `git hash-object` on a FIFO plan would hang | `lstat` first; runner refuses, hook fails open |
| Major | `retrospective.md` | `architecture-reviewer` given a file list, contract needs a diff | Narrowed range diff |
| Minor | `state.md`, `state-health.md` | Digest enforcement overstated, fail-open cases undocumented | Stated precisely; null digest is DRIFT |
| Note | `session-context.mjs` | Rendering could throw past the hook's try | Wrapped |

Acceptance: tests cover each row; `bash scripts/validate.sh` OK; version and CHANGELOG at 0.25.1.
