# v0.24.1: audit fixes

Status: approved by the user on 2026-09-08 ("Let's build everything in!"), from a two-model audit
(Claude Fable and Codex gpt-6-astra, both high effort) of the repository after 0.23 and 0.24.
Writing standard: `plugins/poteto/skills/unslop/SKILL.md`.

## Defects fixed

| Severity | Where | Defect | Fix |
|---|---|---|---|
| High | `scripts/codex/lib/snapshot.mjs` | Hook fingerprints recorded name, size, and mode, so a same-size rewrite of a git hook passed the audit and would run on the PM's next commit | Hash hook content |
| High | same | In a linked worktree `--git-dir` is the private dir, which has no hooks or `info/exclude`, so shared ones went unchecked | Use `--git-common-dir` |
| Medium | `commands/doctor.md` | The preflight line omitted `--mode build`; usage error | Added |
| Medium | `scripts/codex/lib/args.mjs` | `branch` scope required `--base` even for `--preflight` | Required only for a real run |
| Medium | `references/planning-and-signoff.md` | Marker commit before the plan, `git init` only after sign-off | Init offered at planning start |
| Medium | `references/state.md` | "No spec is discovery" misclassified a `tiny` project | Discovery is no spec and no plan |
| Medium | `references/parallel-execution.md` | "No full test run" cannot be passed to `codex-builder` | Applies to `expert-builder`; Codex stories batch only when their verification commands coexist |
| Medium | `references/instruction-layers.md` | Nested `AGENTS.md` and `paths:` rules promised more than they load | Documented the real loading rules |
| Low | `hooks/session-context.mjs` | An empty diff since `BASE_COMMIT` read as stale | Empty is current |
| Low | many | Pre-0.23 and pre-0.24 wording | Removed; validate scan widened |

## Acceptance criteria

- AC-1: a same-size hook rewrite and a shared-hook change seen from a linked worktree both change
  `gitMetadataFingerprint`; tests prove it.
- AC-2: `--mode review --scope branch --preflight` reports ready without `--base`.
- AC-3: the widened legacy scan passes with no exclusions beyond the documented fallbacks.
- AC-4: version and CHANGELOG at 0.24.1; validate and tests green.
